<?xml version="1.0" encoding="UTF-8" ?>
<odoo>
    <record id="action_type_offers" model="ir.actions.act_window">
        <field name="name">offers</field>
        <field name="res_model">property.offers</field>
        <field name="view_mode">list</field>
    </record>

    <record id="view_property_offers_list" model="ir.ui.view">
        <field name="name">property.offers.list</field>
        <field name="model">property.offers</field>
        <field name="arch" type="xml">
            <list string="Offers" editable="top" decoration-success="status=='accepted'" decoration-danger="status=='refused'" >
                <field name="price"/>
                <field name="partner_name"/>
                <field name="validity"/>
                <field name="deadline"/>
                <button name="action_accepted" type="object" icon="fa-check" invisible="status in ('refused','accepted')"/>
                <button name="action_refused" type="object" icon="fa-times" invisible="status in ('refused','accepted')"/>
                <field name="status" optional="hide"/>
            </list>
        </field>
    </record>

</odoo>